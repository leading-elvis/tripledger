# TripLedger 網頁部署與搬遷驗收

## GitHub 保存

- 分支：`refactor/web-sites-prototype`
- 草稿 PR：https://github.com/leading-elvis/tripledger/pull/1
- 功能基線提交：`035ff945d793ae05efdba4ed00cb176271595a4c`
- 已從遠端重新 clone 功能基線，重新安裝鎖定依賴，通過 7 項測試、TypeScript 檢查、Sites Worker 建置及獨立網頁建置。
- 備份檢視入口提交：`31fccf3bc7072d44d2da228be8ad37b0c9e657b8`；新增後再次通過 TypeScript、Sites Worker 及獨立網頁建置。後續 `d2df81b18fa599021dc799bba18309530680d3a4` 僅更新操作說明。
- 第一批流程完善來源：`be159ae33eda4a72a943141e72892452a0f72a46`；本機通過 11 項測試、TypeScript、Sites Worker 及獨立網頁建置。這次未另做遠端乾淨 clone，與上述 7 項基線驗收分開記錄。
- 多人功能來源：`a691ffcad09a6bd67ea406d54a061a7d9329dd54`；本機通過 16 項測試、TypeScript、Sites Worker 及獨立網頁建置。此來源包含 schema 3 與資料庫 migration；本次未重做遠端乾淨 clone。
- 多頁面來源：`29f26e80579b487f3cfcfa2152f2c53930482650`；19 項測試、TypeScript、兩種建置及 Windows 獨立網址／登入檢查通過。帳務與 schema 3 格式未改動。
- 多人付款來源：`726914f2a32b6ece0aceab0f0ca784e9b5fe385f`；26 項測試、TypeScript、兩種建置、schema 4 Windows 還原與 schema 3 升級均通過。
- 舊版 `main` 未修改。使用者帳目、收據、密碼及本機演練檔案不納入 Git。

## 目前 Sites 發布

- 專案：`appgprj_6ab2109b6a24819190d3b371de24f1ad`
- 正式網址：https://tripledger-elvis-lab.workspace-309457.chatgpt.site
- 受眾：`public`，依使用者明確選擇開放網站入口；帳本內容仍由應用程式驗證登入與核准成員資格。access revision 2，更新於 `2026-09-22T09:32:14.592810+00:00`，未增加 Sites 編輯者。
- 狀態：`succeeded`，最新版本 7，完成時間 `2026-09-23T01:44:09.946721+00:00`，維持現有公開入口及帳本授權。
- 最新 GitHub 應用程式來源：`726914f2a32b6ece0aceab0f0ca784e9b5fe385f`。
- 最新 Sites 來源：`eb258892e136f051faad6ff4feebccf94b38ee1a`。
- 最新版本 ID：`appgprj_6ab2109b6a24819190d3b371de24f1ad~appgver_b9d31760bcdc8191a4d93b82c7ab0fb0`。
- 最新部署 ID：`appgdep_6ab32ed6604c8191a45d58f05c3d0205`。
- 公開後以不含登入 cookie 的 HTTP 請求驗證：首頁 `/` 回應 200，`/api/state` 回應 401 與「請先登入」。此項不是第二個真實 ChatGPT 帳號驗收。
- 現有正常登入的站主瀏覽器可載入既有帳本及新版「共同記帳」頁籤，顯示管理者角色與建立邀請入口；此項只讀檢查沒有修改既有帳務。

GitHub 保存整個 monorepo；Sites 保存 `apps/web` 專用來源，兩者 SHA 不同。此驗收文件的後續提交不改動已部署的應用程式。

版本 1 已建立雲端測試資料；版本 2 新增備份檢視入口並完成首次還原測試。版本 3 只同步操作說明。版本 4 完成第一批功能與 schema 2 雲端還原，當時仍僅擁有者可存取。版本 4 Sites 來源為 `5bfe767aeddb36b398b23d3406732a1df3a19cc9`，版本 ID 為 `appgprj_6ab2109b6a24819190d3b371de24f1ad~appgver_4467abe69054819188588a7731e68f8b`，部署 ID 為 `appgdep_6ab22ec59f688191af883792af3ee7e8`，成功於 `2026-09-22T07:31:36.689189+00:00`。

## 多人付款驗收（版本 7）

- 「誰先付款」支援從既有旅伴新增／移除多位付款人及個別金額；付款不等於分攤，兩者各自合計須等於支出總額。至少 1 人、最多該帳本旅伴數，禁止重複旅伴及非正數金額。
- 餘額、還款建議、名稱搜尋、支出明細及更正前後歷史均使用完整付款陣列。記帳人權限仍依 createdBy；參與墊付不會取得他人建立帳目的修改權限。
- 26 項自動化測試、TypeScript、Sites Worker 與 standalone 建置通過。新增覆蓋多付款人／尾差／指定分攤／不參與分攤的付款者、無效金額、更正／作廢、已還款後反向餘額、重送及同時寫入、付款與權限分離、收據／持久化及舊資料升級。
- schema 4 保存 payments，仍接受 schema 1–3 的真實 payerId 資料與歷史快照。JSON document 上限增至 1 MiB ＋ 64 KiB，維持既有寫入預留，避免原本合法且接近滿額的舊帳本因升級增量而無法讀取／備份；1,014,407 bytes 舊測試資料轉為 1,048,607 bytes 後可完整往返。
- 本機 390px 模擬畫面驗證兩位付款、增加至全部旅伴／移除、金額不符拒絕、收據上傳、更正前後明細及餘額，無橫向溢位。支出 1,200 元，甲 800／乙 400，更正成甲 700／乙 500 後，三人平均分攤仍各 400，應收甲 300／乙 100，丙應付 400。總額從 1,000 改 1,100 時，手填甲 600 保留，新增乙預填差額 500。
- 本機 Sites D1/R2 合成帳本 ID `33b1af23-61bb-4aa1-a8d1-bf1c45b9c51c`，schema 4 revision 3，3 位旅伴、1 筆兩人付款支出、1 筆更正、1 位操作人及 1 張 68-byte 收據；透過正常本機 mock 登入匯出，還原到全新 Windows Node 24／SQLite／收據目錄。
- 新版備份 SHA-256：`ac94f916346215fcfb76e239c037886ebee1ca5a8235ca133cfc9e39fdc2f0de`；收據 SHA-256：`c4166024f2e7da975c2c1a06b44962f891b0161538a633f8132dc4667e36153a`。報告 `apps/web/.test-output/cloud-restore-abNwxv/report.json` 通過，完成於 `2026-09-23T01:40:26.408Z`。
- 舊 schema 3 合成帳本另重新還原：保留 2 位操作人、2 筆成員歷史、2 筆還款（1 筆待確認）及收據，單付款者轉為全額付款；報告 `apps/web/.test-output/cloud-restore-K7fada/report.json` 通過，完成於 `2026-09-23T01:40:27.302Z`。
- 兩次還原皆核對每個財務及歷史欄位、權限清除、重複匯入拒絕、收據 bytes／SHA-256、重啟持久化與再次匯出。測試服務已停止；備份、圖片、報告不提交 Git。這是本機合成帳本驗收，不代表新版正式雲端雙帳號或實體 NAS 驗收。

## 多頁面驗收（版本 6）

版本 6 成功於 `2026-09-22T10:37:47.296793+00:00`，Sites 來源 `5e13552a907efefb4f00a97cb403da2fbc8603b0`，版本 ID `appgprj_6ab2109b6a24819190d3b371de24f1ad~appgver_98704f7a6dac8191aaa3233057199c18`，部署 ID `appgdep_6ab25a68b8fc8191a1593fa67aac59ee`。

- 首頁旅程卡片清單，進行中／已封存篩選保存在 query；加入頁 `/join`；帳本支出、分攤、成員及備份各有 `/trips/<id>/<section>` 網址。
- Sites 新增框架頁面入口；Windows 自架入口使用同一份路由解析與 HTML fallback。直接載入／重新整理保留指定帳本及頁面，登入保留返回位置，既有 `/#join=...` 邀請相容。找不到或無權帳本不會顯示另一帳本。
- 19 項測試通過（原 16 項及 3 項路由／登入返回測試），TypeScript、Sites Worker 與 standalone 建置通過。沒有新增資料庫 migration 或改變備份格式。
- 本機瀏覽器驗證四個功能網址、重新整理、上一頁／下一頁與加入頁；成員設定未儲存時切頁提示，取消後保留輸入，明確放棄後開啟目的頁。修正加入表單初始顯示錯誤占位字串，以及外部成員更新誤判成未儲存修改。
- 390px 模擬視窗下首頁卡片及帳本導覽無橫向溢位（文件寬度與可用寬度均為 375px），不視為手機實機驗收。強制關閉、重新登入或手機未觸發瀏覽器離開提示時不保證草稿保存。
- 本機 Sites 六個頁面直接 GET 200、無效 section 404、匿名 API 401。全新 Windows Node 服務六個頁面 200、未知網址 404、匿名 API 401、密碼登入及登入後讀取成功，測試服務已停止。

## 多人功能驗收（版本 5）

版本 5 成功於 `2026-09-22T09:31:49.232896+00:00`，先於 owner-only 受眾發布再開放入口。Sites 來源 `c0a6ba2677ca16ed8a090062341fdaf108317535`，版本 ID `appgprj_6ab2109b6a24819190d3b371de24f1ad~appgver_e5a755be11048191a8ceea4c0938602e`，部署 ID `appgdep_6ab24af18394819192c8f71237cafa61`。下列多人及匿名入口驗收為該版本的結果。

- 邀請連結 7 天有效、單次使用、可撤銷；持連結者登入後申請，核准前無帳本／收據權限。一般首頁網址不授予既有帳本權限。
- 管理者、記帳成員及僅檢視成員由後端驗證權限。操作人與財務旅伴分離；管理者明確核准帳號對應旅伴，不能以同名或 email 自動認領。移除成員阻擋後續 API 存取，保留歷史與餘額。
- 多人新還款待確認，不先扣除餘額；收款方確認後入帳。無帳號綁定的收款旅伴可由管理者代辦，保留操作人、時間及代辦標示；歷史單人還款維持已確認。
- 帳務與有效授權同一次條件版本更新，避免權限撤銷與同時寫入互相覆蓋。可見頁面每 15 秒及重新聚焦時同步；衝突表單保留輸入並要求核對。
- 16 項測試通過：既有 11 項加上邀請／核准／角色、過期／撤銷／單次使用、未加入帳號隔離、收據與備份權限、成員移除及版本競爭、收款方確認／代辦、待確認額度、重送及反向餘額，以及 schema 3 還原權限清除。
- 本機 UI 已建立邀請，核准本機合成申請並綁定旅伴；操作代確認還款，確認待確認款項不影響餘額、入帳後餘額與歷史正確。390px 模擬視窗的可用寬度與文件寬度均為 375px，無橫向溢位。
- 真實雙 ChatGPT 帳號的邀請登入、申請、核准、共同新增／更正與權限撤銷尚待驗收；合成帳號及本機 UI 結果不代替此項。

## 本機多人資料到 Windows 還原（schema 3）

**已通過。來源是本機 Sites D1/R2，不是正式雲端。** 全新 Windows Node 24／SQLite／檔案服務比對 schema 3 原始可攜欄位，避免先正規化時遺漏未知欄位而誤判相同。保留財務旅伴、操作人、成員異動、還款狀態／歷程及收據；有效帳號綁定、邀請和申請不帶入，匯入者成為新 owner。獨立入口目前仍是單一管理者登入，尚未實作自架多人重新認領流程。

- 本機旅程 ID：`810cefc3-8f26-4966-9f29-1c7e6f04d1d3`；schema 3、來源 revision 8。
- 筆數：3 位財務旅伴、2 位操作人、2 筆成員異動、1 筆支出、2 筆還款（1 筆已確認、1 筆待確認）、1 張 68-byte 合成 PNG。
- 完整 JSON SHA-256：`4333a2bd2e0f9a710bdf9d4cf9bd13147d03e93a209db04b0a7c00586926d230`。
- 收據 SHA-256：`c4166024f2e7da975c2c1a06b44962f891b0161538a633f8132dc4667e36153a`。
- 本機報告：`apps/web/.test-output/cloud-restore-giHhQP/report.json`，`passed: true`、`liveAuthorityReset: true`，完成於 `2026-09-22T09:24:12.497Z`。報告資料夾沿用工具名稱，不代表來源為雲端。
- 其他通過項目：匿名拒絕、全部可攜資料與餘額比對、收據校驗、重複匯入 409、重啟持久化、舊 session 失效及再次匯出完整性。測試服務已停止，測試資料不提交至 Git。

## 第一批功能驗收（版本 4）

- 支出更正與作廢歷史、旅程更名／封存／解除封存、搜尋及分類／日期／紀錄狀態篩選；封存後唯讀，仍可備份。
- 保存失敗及版本衝突時保留開啟表單的輸入；最新內容須明確核對後才能重送。另一分頁封存旅程時，草稿仍綁定原旅程，不會切到背景列表的其他旅程。
- 收據選檔／拍照入口、預覽、移除及超限壓縮。JPG／PNG／WebP 超過 1 MiB 時嘗試縮小；本機瀏覽器以 5,763,023-byte 合成圖片驗證壓縮至約 987 KB，並確認移除後能重選同檔。尚未做實機拍照與照片文字清晰度驗收。
- 表單、支出列表、旅程設定及共用 UI 已拆分。草稿只保留在目前開啟的頁面，不承諾重整後保留或離線同步。
- 11 項自動化測試涵蓋三人十筆支出、部分還款、更正／作廢、篩選、衝突、重送、歷史／收據備份與獨立還原；另驗證已還款後更正造成反向債務、封存容量保留解除封存空間、歷史鏈與檔案大小限制。舊 schema 1 備份維持原分攤，schema 2 缺少／竄改歷史會拒絕匯入。
- 本機 UI 已操作更正、搜尋、旅程更名／封存／解除封存、草稿衝突；搜尋無結果時帳本總額仍正確。390px 模擬視窗的可用寬度與文件寬度均為 375px，沒有橫向溢位。這不等於 iPhone／Android 實機驗收。

## 雲端到 Windows 還原（schema 2，版本 4）

**已通過。** 透過正常登入及私人 Sites 網頁另建「更正與封存驗收（已完成）」合成旅程，3 位旅伴平均分攤 TWD 1,200.00，記錄乙還甲 TWD 200.00，再把支出更正為 TWD 600.00。畫面確認甲應收 TWD 200.00、乙已結清、丙應付 TWD 200.00；更正歷史顯示前後金額與分攤，原收據保留。更名並封存後新增按鈕停用、備份仍可用。沒有實際轉帳，也未修改既有旅程的使用者輸入。

沿用「檢視完整備份」取得網站呈現的原始 JSON，保存至本機後執行 `verify-web-backup.mjs`。全新 Windows Node 24／SQLite／檔案服務通過完整可攜帳本逐欄比對，包含封存狀態及 3 筆歷史，並通過匿名拒絕、重複匯入 409、收據 bytes／SHA-256、正常重啟持久化、舊 session 失效與再次匯出驗證。

- 雲端旅程 ID：`5c519408-cc5a-4f1e-b153-7200e2b6a7b4`；schema 2、來源 revision 6。
- 筆數：3 位旅伴、1 筆支出、1 筆部分還款、3 筆歷史（更正／更名／封存）、1 張 68-byte 合成 PNG。
- 匯出時間：`2026-09-22T07:39:54.476Z`。
- 完整 JSON SHA-256：`ba6a0b0bd4070722262bb6c2cff18e7a118c111a5f733dddae80554f85143ccd`。
- 收據 SHA-256：`c4166024f2e7da975c2c1a06b44962f891b0161538a633f8132dc4667e36153a`。
- 本機報告：`apps/web/.test-output/cloud-restore-FFG8KX/report.json`，`passed: true`，完成於 `2026-09-22T07:42:32.602Z`。測試服務已停止；備份、圖片與報告不提交至 Git。

三人十筆流程是自動化測試；本次正式雲端操作是一筆支出的獨立驗收，兩者不混稱。備份保留的是實際入庫的圖片；如果上傳前經壓縮，不能由備份取回壓縮前原圖。直接 Blob 下載與剪貼簿仍待實際瀏覽器驗收。

## 首次雲端到 Windows 還原（schema 1，歷史紀錄）

**已通過。** 在私人正式 Sites 透過正常登入及網頁操作建立「雲端搬遷驗收 2026-09-22」，包含 2 位旅伴、TWD 1,265.00 支出、平均分攤、TWD 200.00 部分還款及 1 張 68-byte 合成 PNG。重新載入網頁後資料仍存在，雙方應收／應付餘額均為 TWD 432.50。這些全是合成測試資料，沒有實際轉帳。

本次內嵌瀏覽器未完成 Blob 檔案下載，因此新增並使用「檢視完整備份」入口：讀取網站呈現的全部原始 JSON，再以 UTF-8 保存至本機。資料由相同的受保護備份 API 產生，包含實際雲端收據 bytes；沒有改寫金額或重建收據，也沒有使用平台驗證繞過 token。**本次不將內嵌瀏覽器的直接下載／剪貼簿操作列為已驗證。**

- 雲端旅程 ID：`58f232d5-3a99-42c0-b942-d800f1950eb3`。
- 備份格式：`tripledger-backup`，schema 1、來源 revision 3。
- 匯出時間：`2026-09-22T06:39:28.460Z`。
- 完整 JSON 檔 SHA-256：`388ebfec1c72459143b4e92795b28263b83894c7608192309860f189ed74f6fe`。
- 收據 SHA-256：`c4166024f2e7da975c2c1a06b44962f891b0161538a633f8132dc4667e36153a`。
- 目的地：Windows、Node.js 24.19.0、全新 SQLite 與本機收據目錄，完全獨立於 Sites runtime。
- 驗證工具：[verify-web-backup.mjs](../scripts/verify-web-backup.mjs)。
- 本機報告：`apps/web/.test-output/cloud-restore-ska8Xo/report.json`，`passed: true`；資料與報告不提交至 Git。

通過項目：匿名存取拒絕、全部帳務與旅伴 ID／分攤／還款保留、雙方餘額一致、所有收據 bytes 與 SHA-256 一致、重複匯入回應 409 且不覆寫、正常停止並重啟後 SQLite／收據持久化、重啟後舊 session 失效、目的地再次匯出完整备份仍可驗證。收據儲存 ID 於匯入時重新配置，以免覆寫現有檔案。

使用 Node 24、依操作說明建置獨立網頁後，可從 GitHub 根目錄重做：

```powershell
node scripts/verify-web-backup.mjs "D:\path\to\backup.json"
```

工具只連 localhost，每次建立新的測試資料夾，結束後停止測試服務；不改動原始備份或雲端帳目。

## 實測界線

- 本次驗證為小型合成帳本；尚未做滿額容量、長時間壓力或大量收據測試。
- 未在實體 NAS／Docker 環境部署。Windows 測試服務僅監聽 loopback，測試完成後已關閉。
- Sites 多人協作已實作並發布，真實雙 ChatGPT 帳號及手機實機驗收尚待完成。自架入口仍是單一管理者；多人帳號重綁遷移、付費權益及公開大眾營運仍待後續版本。
